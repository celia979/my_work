/*
** EPITECH PROJECT, 2025
** my_strcapitalize.c
** File description:
** yes
*/

#include <unistd.h>

char *my_strcapitalize(char *str)
{
    //for (int i = 0; str[i] != '\0'; i++) {
    //if (str[i] >= 'a' && str[i] <= 'z')
    //str[i] -= 32;
    //if (str[i] >= 'A' && str[i] <= 'Z')
    //str[i] += 32;
    //}
    return str;
}

int main(void)
{
    my_strcapitalize(str);
    my_putstr(str);
    return 0;
}

